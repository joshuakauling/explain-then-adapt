# Inference

TODO: Document the inference workflow.

Expected scope:

- Reasoning Model sampling,
- guided and unguided Prediction Model sampling,
- TTT adapter loading,
- per-task inference with and without guidance,
- candidate generation and selection.

This section should make clear which parts use the generated explanation and
which parts rely only on the ARC demonstrations.
