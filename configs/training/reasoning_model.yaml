schema_version: 1
seed: 167

model:
  name: Qwen/Qwen3-4B-Thinking-2507
  revision: null
  quantization_bits: 4
  quantization_type: nf4
  double_quantization: true
  dtype: bfloat16
  attention_implementation: flash_attention_2
  gradient_checkpointing: true

lora:
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  rank: 128
  alpha: 32
  dropout: 0.0
  bias: none
  use_rslora: false
  use_dora: false

data:
  variants_per_task: 100
  max_sequence_length: 8192
  grid_delimiter: ""
  assistant_header: "<|im_start|>assistant\n"
  pad_to_multiple_of: 8

optimization:
  optimizer: adamw_8bit
  scheduler: cosine
  epochs: 100
  micro_batch_size: 2
  gradient_accumulation_steps: 8
  validation_batch_size: 2
  peak_learning_rate: 1.0e-4
  end_learning_rate: 2.0e-5
  warmup_ratio: 0.05
  warmup_start_factor: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.999
  adam_epsilon: 1.0e-8
  weight_decay: 0.0
  max_grad_norm: 1.0

control:
  log_every_steps: 5
  validate_every_steps: 80
  checkpoint_every_steps: 400
  # These epoch boundaries preserve the RM candidates that the 400-step
  # interval alone could not produce.
  checkpoint_epochs: [20, 30]
  checkpoint_steps: []

loader:
  num_workers: 0
  pin_memory: true
  persistent_workers: false

tracking:
  wandb_project: Reasoning_Models
